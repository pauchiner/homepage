---
const {maxWidth} = Astro.props;
const width = maxWidth ?? 600;
---

<div style={`max-width: ${width}px`}>
  <slot />
</div>

<style>
  div {
    width: 100%;
    height: 100%;
    display: flex;
    margin: 0px 20px;
    flex-direction: column;
  }
</style>
